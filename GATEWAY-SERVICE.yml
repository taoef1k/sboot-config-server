spring:
  cloud:
    gateway:
      default-filters:
        - TokenRelay
      routes:
      - id: ORDER-SERVICE
        uri: lb://ORDER-SERVICE
        predicates:
        - Path=/api/orders/**
        filters:
        - RemoveRequestHeader=Cookie
        - name: CircuitBreaker
          args:
            name: ORDER-SERVICE
            fallbackUri: forward:/orderServiceFallback
      - id: BILLING-SERVICE
        uri: lb://BILLING-SERVICE
        predicates:
        - Path=/api/billings/**
        filters:
        - RemoveRequestHeader=Cookie
        - name: CircuitBreaker
          args:
            name: BILLING-SERVICE
            fallbackUri: forward:/billingServiceFallback
  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: http://dell-pc:8777/auth/realms/demo
            user-name-attribute: preferred_username
        registration:
          keycloak:
            client-id: springboot-microservice
            client-secret: 774cfa5e-1277-4c4c-8224-aff51ef43eff

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream

logging:
  level:
    org:
      springframework:
        cloud.gateway: DEBUG
        http.server.reactive: DEBUG
        web.reactive: DEBUG
